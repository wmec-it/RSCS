# TODO: Add test for if regular release directory exists, and if it should be copied to both dirs, or just one
Copy-Item -Path "./vc_runtime.bat" -Destination "./target/x86_64-pc-windows-msvc/release/vc_runtime.bat" -Force -Recurse